name: Preview Pages

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages-preview-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        # actions/checkout@v6.0.2
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false

      - name: Set up Node.js
        # actions/setup-node@v7.0.0
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Audit production dependency graph
        run: npm run audit:ci

      - name: Build deployable site
        run: npm run build:dist

      - name: Upload preview payload
        # actions/upload-artifact@v6.0.0
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f
        with:
          name: preview-dist-${{ github.run_id }}
          path: dist/
          if-no-files-found: error
          retention-days: 3

  preview:
    needs: build
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      ready: ${{ steps.cloudflare.outputs.ready }}
      preview_url: ${{ steps.deploy.outputs.url }}
    environment:
      name: preview
      url: ${{ steps.deploy.outputs.url }}
    steps:
      - name: Set up trusted Node.js runtime
        # actions/setup-node@v7.0.0
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 22

      - name: Download preview payload
        # actions/download-artifact@v6.0.0
        uses: actions/download-artifact@018cc2cf5baa6db3ef3c5f8a56943fffe632ef53
        with:
          name: preview-dist-${{ github.run_id }}
          path: dist

      - name: Check preview deploy credential presence
        id: cloudflare
        shell: bash
        env:
          CLOUDFLARE_PAGES_API_TOKEN: ${{ secrets.CLOUDFLARE_PAGES_API_TOKEN }}
          CLOUDFLARE_API_TOKEN_LEGACY: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID_VARIABLE: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_ACCOUNT_ID_LEGACY: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          set -euo pipefail
          export CLOUDFLARE_API_TOKEN="${CLOUDFLARE_PAGES_API_TOKEN:-$CLOUDFLARE_API_TOKEN_LEGACY}"
          export CLOUDFLARE_ACCOUNT_ID="${CLOUDFLARE_ACCOUNT_ID_VARIABLE:-$CLOUDFLARE_ACCOUNT_ID_LEGACY}"

          if [ -z "$CLOUDFLARE_API_TOKEN" ] || [ -z "$CLOUDFLARE_ACCOUNT_ID" ]; then
            echo "ready=false" >> "$GITHUB_OUTPUT"
            echo "::notice::Cloudflare preview credentials are not configured; skipping preview deploy."
            exit 0
          fi
          if [ -z "$CLOUDFLARE_PAGES_API_TOKEN" ]; then
            echo "::warning::Using legacy CLOUDFLARE_API_TOKEN; migrate it to the preview environment as CLOUDFLARE_PAGES_API_TOKEN."
          fi
          if [ -z "$CLOUDFLARE_ACCOUNT_ID_VARIABLE" ]; then
            echo "::warning::Using legacy CLOUDFLARE_ACCOUNT_ID secret; migrate it to a preview environment variable."
          fi
          echo "ready=true" >> "$GITHUB_OUTPUT"

      - name: Install pinned Wrangler
        if: steps.cloudflare.outputs.ready == 'true'
        run: npm install --global --ignore-scripts --no-audit --no-fund wrangler@4.114.0

      - name: Validate preview deploy credentials
        if: steps.cloudflare.outputs.ready == 'true'
        shell: bash
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_PAGES_API_TOKEN || secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID || secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          set -euo pipefail
          if ! wrangler pages project list >/dev/null 2>&1; then
            echo "::error::Cloudflare deploy credentials are set but invalid. Check the preview CLOUDFLARE_PAGES_API_TOKEN secret and CLOUDFLARE_ACCOUNT_ID variable."
            exit 1
          fi

      - name: Deploy preview to Cloudflare Pages
        if: steps.cloudflare.outputs.ready == 'true'
        id: deploy
        shell: bash
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_PAGES_API_TOKEN || secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID || secrets.CLOUDFLARE_ACCOUNT_ID }}
          BRANCH_REF: ${{ github.head_ref || github.ref_name }}
        run: |
          set -euo pipefail
          raw_branch="$BRANCH_REF"
          sanitized="$(printf '%s' "$raw_branch" | tr '[:upper:]' '[:lower:]' | tr -c 'a-z0-9-' '-' | sed -E 's/-+/-/g; s/^-//; s/-$//')"
          # Cloudflare Pages limits subdomains to 28 chars.
          branch_slug="$(printf '%s' "$sanitized" | cut -c1-20 | sed -E 's/-$//')"
          if [ -z "$branch_slug" ]; then
            branch_slug="run-${GITHUB_RUN_ID}"
          fi
          # A preview token must never be able to publish the production branch.
          short_branch="preview-${branch_slug}"
          echo "Raw branch:        $raw_branch"
          echo "Sanitized branch:  $short_branch"
          output="$(wrangler pages deploy dist --project-name=alexnava-me --branch="$short_branch" 2>&1)"
          printf '%s\n' "$output"
          url="$(printf '%s' "$output" | grep -oE 'https://[a-z0-9-]+\.alexnava-me\.pages\.dev' | tail -1)"
          if [ -z "$url" ]; then
            echo "::error::Could not parse preview URL from wrangler output."
            exit 1
          fi
          echo "url=$url" >> "$GITHUB_OUTPUT"
          echo "## Preview" >> "$GITHUB_STEP_SUMMARY"
          echo "$url" >> "$GITHUB_STEP_SUMMARY"

      - name: Smoke-check preview
        if: steps.cloudflare.outputs.ready == 'true'
        shell: bash
        env:
          PREVIEW_URL: ${{ steps.deploy.outputs.url }}
        run: |
          set -euo pipefail
          body="$RUNNER_TEMP/preview-body.html"
          headers="$RUNNER_TEMP/preview-headers.txt"
          expected_host="${PREVIEW_URL#https://}"
          expected_host="${expected_host%%/*}"
          if ! printf '%s' "$expected_host" | grep -Eq '^[a-z0-9-]+\.alexnava-me\.pages\.dev$'; then
            echo "::error::Unexpected preview deployment host: $expected_host"
            exit 1
          fi

          for attempt in 1 2 3 4 5 6; do
            : > "$headers"
            result="$(
              curl \
              --silent \
              --show-error \
              --connect-timeout 10 \
              --max-time 20 \
              --dump-header "$headers" \
              --output "$body" \
              --write-out '%{http_code}\t%{url_effective}' \
              "$PREVIEW_URL" ||
                true
            )"
            status="${result%%$'\t'*}"
            effective_url="${result#*$'\t'}"
            effective_host="${effective_url#https://}"
            effective_host="${effective_host%%/*}"
            if [ "$status" = "200" ] &&
              [ "$effective_host" = "$expected_host" ] &&
              grep -Fq "Calm by design." "$body"; then
              echo "Preview smoke check passed."
              exit 0
            fi
            if [ "$attempt" -lt 6 ]; then
              echo "Preview smoke attempt $attempt failed; retrying in 5 seconds."
              sleep 5
            fi
          done

          echo "::error::Preview smoke check failed after 6 attempts."
          exit 1

  comment:
    if: github.event_name == 'pull_request' && needs.preview.outputs.ready == 'true'
    needs: preview
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Comment preview URL on PR
        # actions/github-script@v9.0.0
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3
        env:
          PREVIEW_URL: ${{ needs.preview.outputs.preview_url }}
          COMMIT_SHA: ${{ github.event.pull_request.head.sha }}
        with:
          script: |
            const marker = '<!-- preview-pages-comment -->';
            const body = [
              marker,
              '### Cloudflare Pages preview',
              '',
              `${process.env.PREVIEW_URL}`,
              '',
              `Latest commit: \`${process.env.COMMIT_SHA.slice(0, 7)}\` · built by [Preview Pages](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})`,
            ].join('\n');

            const { owner, repo } = context.repo;
            const issue_number = context.issue.number;
            const comments = await github.paginate(github.rest.issues.listComments, {
              owner, repo, issue_number, per_page: 100,
            });
            const existing = comments.find(
              (c) => c.user?.login === 'github-actions[bot]' && c.body?.includes(marker),
            );
            if (existing) {
              await github.rest.issues.updateComment({ owner, repo, comment_id: existing.id, body });
            } else {
              await github.rest.issues.createComment({ owner, repo, issue_number, body });
            }
